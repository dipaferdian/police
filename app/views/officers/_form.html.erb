<%= form_with(model: officer) do |form| %>
  <% if officer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(officer.errors.count, "error") %> prohibited this officer from being saved:</h2>

      <ul>
        <% officer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <form class="form-inline">
      <div class="form-group mb-2">
        <div class="form-group">
          <%= form.label :name,  style: "display: block" %>
          <%= form.text_field :name, class: "form-control", id: "formGroupExampleInput",  placeholder: "String" %>
        </div>
      </div>
      <%= form.submit "Create", class: "btn btn-primary" %>
    </form>
<% end %>


<script>
  myButton = document.getElementsByName("commit")
      myButton.classList.add("");
</script>